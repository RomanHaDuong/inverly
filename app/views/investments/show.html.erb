



  <h1 class="text-center my-4"><%= @investment.name %></h1>
<div class="p-4">
  <div class="shadow d-flex justify-content-center mb-3">

  <div class="col-12 justify-content-center "><%= line_chart @cumul_count%> </div>
  </div>
  <div class="d-flex">
    <div class="col-4">
      <div class="me-3 shadow d-flex justify-content-center">
        <div class="group-card-all p-3" >
          <p class="card-title">If you invest in...</p>
          <div class="mb-3">
            <button class=   > <%= @investment.etf.name %> <%= @investment.etf.ticker_symbol %>
          </div>
          <div class="mb-3">
            <strong>Average Return:</strong> <%= @investment.etf.average_return %>%
          </div>
          <div class="mb-3">
            <strong>Risk Level:</strong>
            <p class="btn btn-dropdown"><%= @investment.risk_level %></p>
          </div>
          <div class="mb-3">
            <strong>Effective Return:</strong>
            <p class="btn btn-primary"><%= @investment.etf.average_return %></p>
          </div>
          <div class="mb-3">
            <strong>Current Price:</strong> $<%= @investment.etf.current_price %>
          </div>
          <%= link_to 'Back to Investments', investments_path, class: 'btn btn-secondary' %>
        </div>
      </div>
    </div>
    <div class="col-8">
      <div class="shadow d-flex justify-content-center">
        <div class="group-card-all p-3" >
          <h5 class="card-title">If you invest in...</h5>
          <div class="mb-3">
            <%= @investment.etf.name %> <%= @investment.etf.ticker_symbol %>
          </div>
          <div class="mb-3">
            <strong>Average Return:</strong> <%= @investment.etf.average_return %>%
          </div>
          <div class="mb-3">
            <strong>Risk Level:</strong>
            <p class="btn btn-dropdown"><%= @investment.risk_level %></p>
          </div>
          <div class="mb-3">
            <strong>Effective Return:</strong>
            <p class="btn btn-primary"><%= @investment.etf.average_return %></p>
          </div>
          <div class="mb-3">
            <strong>Current Price:</strong> $<%= @investment.etf.current_price %>
          </div>
          <%= link_to 'Back to Investments', investments_path, class: 'btn btn-secondary' %>
          </div>
      </div>
    </div>
  </div>
</div>
